{
  "probe_id": "public_pilot_log_redacts_private_payloads",
  "taxonomy": "privacy_containment",
  "boundary": "review item / test fixture",
  "canonical": false,
  "scenario": "A sanitized pilot run log is prepared for commit and accidentally includes a private copied-photo path, raw card text, OCR payload, AI draft, database path, backup path, or generated workbook path.",
  "expected": {
    "boundary": "review item",
    "must_route_to_review": true,
    "must_preserve_traceability": true,
    "must_not_write_canonical": true,
    "external_inference_policy": "local_only"
  }
}
